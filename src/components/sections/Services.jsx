import SectionTitle from "../ui/SectionTitle";

const Services = () => {
  return (
    <div>
      <SectionTitle maintext="Quick overview for services" />
    </div>
  );
};

export default Services;
